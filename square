#include<stdio.h>
int main()
{
   float side,area;
   printf("enter the length of the any side:");
   scanf("%f",&side);
   area=side*side;
   printf("the area of the square:%.2f",area);
   return 0;
}
